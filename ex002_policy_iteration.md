# ex002: 完全な環境モデルを用いた動的計画法1 — 方策反復法(Policy Iteration)

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/yryo1005/TicTacToe_Reinforcement_Learning_training/blob/main/ex002_policy_iteration.ipynb)

## 1. 目的

本実験は，強化学習教材の第2段階である．ex001では環境と評価基盤を構築し，
ランダム方策の性能をベースラインとして測定した．本実験では，ex001で定式化した
「後手が一様ランダム方策に従う既知の環境モデル」を用いて，動的計画法の代表的手法である
方策反復法(Policy Iteration)を実装し，最適方策を厳密に求める．
ex001との主な差分は，環境との相互作用(サンプリング)によってではなく，
既知の遷移確率 $ p(s'\mid s,a) $ を用いた全状態の一括更新によって方策を最適化する点である．


## 共通コンポーネントの定義

以下は全実験(ex001〜ex006)で共通して用いる骨格である．`set_seed`，`ResultLogger`，`TicTacToeEnv`，`BaseAgent`系のクラス，`evaluate`，`play_human_vs_ai` は全実験で同一であり，実験ごとに変更するのはエージェントの価値更新アルゴリズムのみである．


## 2. 手法

### 2.1 環境・エージェント定義

状態・行動空間・環境モデルはex001と同一である．
本実験では，ex001の`TicTacToeEnv.transition_model(s, a)`が返す遷移確率 $ p(s'\mid s,a) $ と
即時報酬 $ r(s,a,s') $ を直接用いる点が特徴である．

### 2.2 目的関数・更新式

**方策評価(Policy Evaluation)**: 現在の決定的方策 $ \pi $ のもとでのベルマン期待方程式

$$
V^{\pi}_{k+1}(s) = \sum_{s'} p\!\left(s' \mid s, \pi(s)\right) \Big[ r\!\left(s, \pi(s), s'\right) + \gamma \, V^{\pi}_k(s') \Big]
$$

を，$ \max_s \left| V^{\pi}_{k+1}(s) - V^{\pi}_k(s) \right| < \theta $ となるまで反復して $ V^{\pi} $ を求める．
ここで $ \gamma \in [0,1) $ は割引率，$ \theta $ は収束判定閾値である．

**方策改善(Policy Improvement)**: 収束した $ V^{\pi} $ を用いて，各状態で1手先読みした
行動価値

$$
Q^{\pi}(s,a) = \sum_{s'} p(s' \mid s,a) \Big[ r(s,a,s') + \gamma V^{\pi}(s') \Big]
$$

を計算し，$ \pi'(s) = \arg\max_a Q^{\pi}(s,a) $ に方策を更新する．
方策評価と方策改善を，方策が変化しなくなる($ \pi' = \pi $)まで交互に繰り返す．

コード上では，`policy_evaluation_sweep`関数の`v_new`が上式の $ V^{\pi}_{k+1}(s) $ に，
`q_from_value`関数の`Q[s][a]`が上式の $ Q^{\pi}(s,a) $ に対応する．

### 2.3 評価指標

学習過程の評価には，方策評価の各スイープにおける価値関数の最大変化量
$ \max_s |V_{k+1}(s) - V_k(s)| $ (収束曲線)を用いる．
最終的な方策の強さは，ex001と同じ`evaluate`関数による対ランダム方策の勝率・引き分け率・敗率で測る．



## 3. 実験条件

| 項目 | 値 | 備考 |
| :--- | :--- | :--- |
| 環境 | 3目並べ(先手Xがエージェント，後手Oは一様ランダム方策) | ex001と共通 |
| 割引率 $ \gamma $ | 0.95 | 有限手数の終端ゲームのため $ \gamma=1.0 $ でも収束するが，手数による価値の減衰を可視化するため採用 |
| 収束判定閾値 $ \theta $ | 1e-4 | 状態数が2,423と少なく，十分小さい閾値でも現実的な反復回数で収束する |
| シード | 0, 1, 2 | 初期方策(ランダムに生成)のみを変え，収束先が初期値に依存しないことを確認する |
| 結果の保存先 | `outputs/ex002_tictactoe_dp/PolicyIteration/gamma0.95_theta0.0001/seed_{k}/` | |

### 実験前に考える

1. 初期方策をランダムに変えた場合(シードを変えた場合)，方策反復法が最終的に収束する価値関数・方策はシードに依存すると予想するか，しないと予想するか．理由を述べよ．
2. 方策反復法の外側ループ(方策評価→方策改善)の反復回数は，何回程度で収束すると予想するか．
3. 得られる最適方策を用いた場合，対ランダム方策の勝率・引き分け率・敗率はex001のランダム方策と比べてどう変化すると予想するか．


## 方策反復法の実装


## 4. 実験結果

<details>
<summary>予想を書いてから開いてください</summary>

以下のセルを実行すると，各シードにおける方策評価スイープごとの価値関数の最大変化量(収束曲線)と，
収束後の方策の対ランダム方策成績が得られる．

</details>



### 実験後に考える

1. 異なるシード(初期方策)から出発しても，最終的な価値関数・方策・勝率は一致していたか．方策反復法が大域最適に収束することの理論的根拠(有限MDP・ベルマン最適作用素の縮小写像性)と結びつけて説明せよ．
2. 収束曲線を見ると，方策評価の内側ループはどのような減衰の仕方をしていたか(線形・指数的など)．
3. ex001のランダム方策と比較して，最適方策の勝率・敗率はどの程度変化したか．3目並べが「先手・後手ともに最適なら引き分け」というゲーム理論的性質を持つにもかかわらず，ランダムな後手に対して勝率が引き分け率を大きく上回る理由を述べよ．



## 5. 考察

<details>
<summary>考察の例を見る前に，自分の考察を書いてください</summary>

方策反復法は，有限状態・有限行動のMDPにおいて，ベルマン最適作用素が縮小写像であることから，
初期方策によらず一意の最適価値関数・最適方策(複数存在する場合は同一の最適価値を与える方策集合の1つ)
に収束することが理論的に保証されている．本実験の結果が初期方策(シード)によらず一致したことは，
この理論的性質と整合する．また，方策評価は反復的な数値計算であるため，各スイープの価値関数の
最大変化量は幾何的に減衰し，$ \theta $ を十分小さく取ることで実用上厳密な最適解に到達できる．
これは，環境モデルが既知である場合に限って可能な，モデルベース手法特有の強みである．
一方で，本実験の計算量は状態数(2,423)と行動数(最大9)の積に比例しており，
状態数が爆発的に増加する複雑な問題ではこの厳密な反復計算が困難になる．
この限界が，ex004以降のモデルフリー手法(モンテカルロ法・Q学習)を導入する動機となる．

</details>



## 6. まとめ

- 既知の環境モデル(固定ランダム方策の後手)を用いた方策反復法を実装し，最適価値関数・最適方策を求めた．
- 異なる初期方策(シード)から出発しても同一の最適解に収束することを確認し，方策反復法の収束保証を実証的に確認した．
- 収束後の方策はex001のランダム方策よりも高い勝率を示し，動的計画法による最適化の効果を確認した．
- 状態数に比例する計算量が，モデルフリー手法(ex004・ex005)を導入する動機であることを述べた．



## 7. 発展課題

**課題**: 割引率を $ \gamma = 1.0 $ に変更して方策反復法を再実行し，$ \gamma = 0.95 $ の場合の結果と比較せよ．

**比較する対象**: $ \gamma=0.95 $ と $ \gamma=1.0 $ それぞれで得られる価値関数の値そのもの，最適方策(選択される行動)，反復回数．

**予想**: 3目並べは高々9手で終了する有限ホライズンのゲームである．割引率を変えることで，最適「方策」自体は変化するか，価値の「値」だけが変化するかを予想せよ．

<details>
<summary>回答例を見る</summary>

3目並べのように必ず有限ステップで終端する決定的なゲーム木構造を持つ問題では，
$ \gamma $ を $ (0,1] $ の範囲で変化させても，各状態での行動の優劣関係(勝ちにつながる手が最も価値が高い，という順序関係)は変わらないため，最適方策そのものは変化しないことが多い．
一方で，価値関数の絶対値は $ \gamma $ に依存し，$ \gamma < 1 $ では勝利までの手数が短いほど価値が高くなる(早い勝利を好む)効果が価値関数の値に表れる．
この違いは，価値関数の大小関係だけでなく，学習の収束速度や，関数近似を用いる設定における汎化性にも影響し得る点で実務上重要である．

</details>

